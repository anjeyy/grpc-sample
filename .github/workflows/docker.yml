name: Publish Docker images
on:
  #  uncomment after workflow is working
  #  release:
  #    types: [ published ]
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push [Client] to Docker Hub
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./grpc-client/Dockerfile
          tags: anjeyy/unbendable:grpc-sample-client-latest
          push: true
      - name: Push [Server] to Docker Hub
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./grpc-server/Dockerfile
          tags: anjeyy/unbendable:grpc-sample-client-latest
          push: true

#TODO dockerfile and reference to dockerfile - https://www.docker.com/blog/docker-v2-github-action-is-now-ga/
